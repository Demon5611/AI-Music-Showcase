# Use global pnpm store instead of project-local .pnpm-store/
# macOS default path. On Linux override globally:
#   pnpm config set store-dir ~/.local/share/pnpm/store/v10 --global
store-dir=~/Library/pnpm/store/v10

shared-workspace-lockfile=true
