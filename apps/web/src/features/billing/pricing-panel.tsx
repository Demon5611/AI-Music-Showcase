"use client";

import { CreditPackagesPricingPanel } from "@/features/billing/credit-packages-pricing-panel";

/**
 * Public pricing entry — prepaid credit packs via TBC only.
 */
export function PricingPanel() {
  return <CreditPackagesPricingPanel />;
}
